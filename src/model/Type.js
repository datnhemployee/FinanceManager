import Color from "../styles/Color";

export default class Type {
    constructor () {
    }

    static default () {
        return {
            name: `Rỗng`,
            isIncome: false,
            color: Color.Gray,
        }
    }
}