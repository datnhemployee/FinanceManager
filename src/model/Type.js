import Color from "../styles/Color";

export default class Type {
    constructor () {
    }

    static default () {
        return {
            name: ``,
            isIncome: false,
            color: Color.Gray,
        }
    }
}