
import React, { Component } from 'react';
import render from './Note.design';

export default class extends Component {
    constructor (props) {
        super(props);
    }

    render() {
        let {
        } = this.props;
        return render();
    }
}
